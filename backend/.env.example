# Port used only for local development (npm run dev / npm start).
# Vercel ignores this and manages the port itself in production.
PORT=3000

# Comma-separated list of allowed frontend origins for CORS.
# Use "*" to allow any origin (fine for local dev / demos, not recommended for production).
FRONTEND_ORIGIN=*

# Standard Node environment flag.
NODE_ENV=development
